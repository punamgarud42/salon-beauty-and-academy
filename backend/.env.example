# Copy this file to .env and fill in real values before running the server.

PORT=5000

# MongoDB connection string. For local dev: mongodb://127.0.0.1:27017/verme
# For hosted: use a MongoDB Atlas connection string.
MONGO_URI=mongodb://db_user:Trisha%4011@ac-fcxyqhf-shard-00-00.tl9lpom.mongodb.net:27017,ac-fcxyqhf-shard-00-01.tl9lpom.mongodb.net:27017,ac-fcxyqhf-shard-00-02.tl9lpom.mongodb.net:27017/salon-academy?ssl=true&replicaSet=atlas-eaudz8-shard-0&authSource=admin&retryWrites=true&w=majority&appName=Cluster0


# Comma-separated list of origins allowed to call this API.
# In dev this is your Vite dev server; in production, your deployed frontend URL.
CORS_ORIGIN=http://localhost:5173

# ---- Added in later phases — not needed yet, kept here so the file only grows once ----

# Phase 9 (Payments) — Razorpay: https://dashboard.razorpay.com/app/keys
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=

# Phase 9 (Payments) — Stripe (if you choose Stripe instead of/alongside Razorpay)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=

# Phase 3/4 confirmations — email (e.g. via SMTP or a provider like Resend/SendGrid)
# EMAIL_FROM=
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=

# Phase 3/4 confirmations — SMS (e.g. via Twilio or an Indian provider like MSG91)
# SMS_API_KEY=
# SMS_SENDER_ID=

# Phase 8 (Admin Dashboard) — auth for the owner login
# JWT_SECRET=
# ADMIN_EMAIL=
# ADMIN_PASSWORD_HASH=
