# Copy to .env for local dev (Vite loads .env automatically).
# Point this at your deployed backend once Phase 8/9 are live; defaults to
# localhost so `npm run dev` works against the Phase 1 backend unchanged.
VITE_API_URL=http://localhost:5000/api
